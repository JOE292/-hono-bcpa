
# Hono Modular REST API (Drizzle)

## Features
- Modular sub-routing (`/users`, `/posts`, `/comments`)
- Service layer abstraction
- Relational DB with FK constraints:
  - user -> posts
  - post -> comments

## Setup

1. Install dependencies
```bash
npm install
